pydantic-ai[mistral]
pydantic
numpy
python-dotenv
tenacity
httpx
fastembed==0.5.1  # ВАЖНО: не апгрейдить без пересчёта DUPLICATE_THRESHOLD в config.py —
                  # версии новее меняют pooling (CLS -> mean) для этой модели, из-за чего
                  # меняется распределение косинусных сходств и калиброванный порог 0.59
                  # перестаёт значить то же самое (см. calibrate_threshold.py)
mcp
pytest
playwright
fastapi
uvicorn
streamlit
requests
pandas