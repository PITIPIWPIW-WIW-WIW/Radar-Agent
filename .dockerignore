venv/
.venv/
__pycache__/
*.pyc
.git/
.pytest_cache/

# Локальные БД и логи — в контейнере это volume, не хотим тащить
# твою локальную историю прогонов внутрь образа
app_database.db
app.log
errors.log

# .env содержит секреты — прокидывается через env_file в docker-compose,
# в образ пересобираться не должен, чтобы не протухал при ребилде
.env
